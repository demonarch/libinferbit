#include "inferbit.h"

const char* inferbit_version(void)       { return INFERBIT_VERSION_STRING; }
int         inferbit_version_major(void) { return INFERBIT_VERSION_MAJOR; }
int         inferbit_version_minor(void) { return INFERBIT_VERSION_MINOR; }
int         inferbit_version_patch(void) { return INFERBIT_VERSION_PATCH; }
